include ../rateButton/rateButton.pug
include ../UI-kit/numberRoomAndPrice/numberRoomAndPrice.pug

mixin room({id, number, type = '', price, stars, review, array = []})
  .room 
    .room__top 
      .swiper
        .swiper-wrapper
          each item in array
            .swiper-slide
              img(src=require("../../images/room2.png"))
        .swiper-pagination
    .room__bot
      .room__info 
        +numberRoomAndPrice({price: price, type: type})
      .room__score 
        .room__stars
          +rateButton({id: 'cardsRateButton1'})
        .room__review
          span= `${review} `
          span Отзывов
